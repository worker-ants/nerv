# 자가호스팅 프로필에서 NERV_EMBED_URL=http://nerv-embed/v1 이 이 이름을 계약한다.
apiVersion: v1
kind: Service
metadata:
  name: nerv-embed
  labels: { app: nerv-embed }
spec:
  selector: { app: nerv-embed }
  ports:
    - { name: http, port: 80, targetPort: http }
