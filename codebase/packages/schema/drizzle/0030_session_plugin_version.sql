-- 이 세션을 연 플러그인의 버전 (2026-09-24 · E12-S03 · database.md §2.6)
--
-- 백로그 E12-S03 의 수용 기준이 "관리형 settings 로 배포되면 참여 호스트의 **활성화 여부를
-- 서버에서 확인** 가능하게 한다" 인데, 서버는 그것을 알 방법이 없었다 — 세션은 훅(플러그인)으로도
-- MCP `nerv_bootstrap`(플러그인 없이)으로도 들어오고, 둘을 가르는 칸이 없었다.
-- 훅 포워더가 `X-NERV-Plugin` 헤더로 버전을 싣고 여기에 남긴다. **값이 있다는 것 자체가
-- 증거다** — 그래서 백필하지 않는다: 옛 세션이 플러그인으로 열렸는지는 이제 알 수 없고,
-- 추정으로 채우면 활성화율이 거짓이 된다.
ALTER TABLE "agent_session" ADD COLUMN "plugin_version" text;
