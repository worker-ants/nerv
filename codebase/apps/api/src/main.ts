// HTTP 엔트리 — Nest(Fastify) 부트스트랩: REST + MCP + WS + SSE + ingest.
//
// E01-S01 의 자리표시자다. 실제 부트스트랩(AppModule 조립·표면 5종 배선)은 E01-S02 가 채운다 —
// 트리와 모듈 맵 정본은 docs/04-mvp/codebase.md §2.2.

export function main(): void {
  throw new Error('E01-S02 에서 구현한다 — NestJS(Fastify) 부트스트랩');
}
