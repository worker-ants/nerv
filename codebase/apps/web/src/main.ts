// 웹 SPA 엔트리 — E01-S03 이 채운다(index.html · vite.config.ts · TanStack Router 파일 라우트).
//
// 라우팅 맵·앱 셸 정본은 docs/04-mvp/screens.md §1.2·§1.3,
// 실시간 계약(WS `/ws` 룸 join → TanStack Query 무효화)은 같은 문서 §1.4 + docs/04-mvp/api.md §3.

export function mount(): void {
  throw new Error('E01-S03 에서 구현한다 — Vite + React SPA 앱 셸');
}
