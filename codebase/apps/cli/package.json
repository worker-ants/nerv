{
  "name": "@nerv/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NERV 임포터 CLI — 원본 체크아웃이 있는 장비에서 실행하는 API 클라이언트 (docs/04-mvp/codebase.md §1.3)",
  "bin": {
    "nerv": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@nerv/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  }
}
