# 의존성·빌드 산출물
node_modules/
dist/
.tsbuild/
.tsbuild-spec/
*.tsbuildinfo

# 로컬 환경 — .env.example 만 커밋한다 (docs/04-mvp/codebase.md §5.2)
.env
.env.local

# 테스트 산출물
coverage/
playwright-report/
test-results/

# 도구 캐시
.vite/
.eslintcache

# 스파이크 산출물 — 리포트는 실행 때마다 갱신된다(측정값은 문서에 기록한다)
apps/web/.spike/
