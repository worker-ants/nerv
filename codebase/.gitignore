# 의존성·빌드 산출물
node_modules/
dist/
.tsbuild/
.tsbuild-spec/
*.tsbuildinfo

# 로컬 환경 — .env.example 만 커밋한다 (docs/04-mvp/codebase.md §5.2)
.env
.env.local

# 테스트 산출물
# E2E 스택의 세션별 포트·프로젝트 이름 (scripts/e2e-stack.mjs)
.e2e/
coverage/
playwright-report/
test-results/

# 도구 캐시
.vite/
.eslintcache

# 스파이크 산출물 — 리포트는 실행 때마다 갱신된다(측정값은 문서에 기록한다)
apps/web/.spike/

# 임포트 리포트 — 실행마다 새로 쓰이는 산출물이다(importer.md §4)
nerv-import-report/
