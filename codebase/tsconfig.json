{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "솔루션 파일 — CI 의 `pnpm exec tsc -b` 진입점(docs/04-mvp/codebase.md §4.5). 코드는 없고 참조만 갖는다.",
  "files": [],
  "references": [
    { "path": "./packages/schema" },
    { "path": "./apps/api" },
    { "path": "./apps/cli" },
    { "path": "./apps/web" }
  ]
}
