# NERV(가칭) — AI 에이전트 협업 개발 플랫폼 제안서

> **요약** — NERV는 기획자·디자이너·개발자·QA가 하나의 플랫폼에서 **스펙 문서를 단일 진실**로 관리하고, Claude Code·Codex 같은 AI 에이전트를 **MCP·훅·스킬로 연동**해 스펙 작성→검토→구현→테스트를 수행하며, 사람은 **승인/거절/코멘트 게이트**를 지키고 **누구(hostname)의 어떤 에이전트 세션이 무엇을 하는지** 실시간으로 보는 멀티 프로젝트 × 멀티 유저(n:n) 협업 플랫폼이다. 이 제안서는 기존 1인용 하네스(clemvion)의 실측 분석과 웹 딥리서치(도구 생태계·협업 플랫폼·연동 기술·저장 전략·HITL·실전 사례)를 근거로 문제 정의부터 아키텍처·데이터 모델·연동 설계·화면·로드맵까지를 다룬다.
>
> 문서 버전 v1.32 · 2026-08-24 · 사람이 읽기 좋은 HTML 판: [html/index.html](html/index.html)
>
> v1.32 변경(2026-08-24 — 관계를 방향으로 가른다, 사람 지시): S3 우측 레일의 `관계` 탭 아래에 **전체 · 역참조 · 레퍼런스** 하위 탭과 건수를 둔다(4.5 §2.4 · REQ-WEB-077). 두 방향은 다른 질문이다 — 역참조는 "이 문서를 고치면 무엇이 흔들리나", 레퍼런스는 "이 문서가 무엇에 기대나". 한 목록에 섞이면 둘 중 하나를 보려고 전체를 훑어야 한다(실측: `0-overview` 50건 = 역참조 22 + 레퍼런스 28). 건수는 **누르기 전에** 보이고, 역참조 설명은 역참조가 보이는 목록에 있을 때만 단다. 제품 매뉴얼의 "관계와 역참조" 절도 같은 커밋에서 고쳤다(AGENTS.md 구현 규약 5).
>
> v1.31 변경(2026-08-24 — 권한 판정이 화면과 서버에서 갈라져 있었다, 사람 보고): "admin 계정인데 스펙 메타 편집이 잠겨 있다"를 좇았다. **화면이 멤버십 한 행만 보고 역할을 판정**하고 있었고, 조직 단위로만 소속된 admin 은 `project_slug === null` 행 하나뿐이라 프로젝트 슬러그로 찾으면 걸리지 않았다 — 서버는 이미 합집합이었으므로 화면이 **서버가 허용할 일을 못 한다고 말하고 있었다**. 같은 뿌리로 네 화면이 틀려 있었다(메타 편집 · 게이트 정책 · 토큰 발급 · 리뷰 처분). 게이트 탭은 없는 필드(`membership.role` 단수)를 읽어 **누구에게나** 읽기 전용이었다. 스코프·역할 판정을 한 곳으로 모으고(4.5 §1.8 · REQ-WEB-075·076), 그 과정에서 **서버 쪽 결함도 하나 찾았다**: `assertMembership` 이 조직 경계를 보지 않아 A 조직의 조직 단위 admin 이 **B 조직의 프로젝트에서도 admin** 이었다(4.4 §1.6a · L2 회귀로 재현·수정). REST·WS·SSE 가 공유하는 판정이라 세 표면이 함께 샜다.
>
> v1.30 변경(2026-08-24 — 설치 절차를 매뉴얼에 넣는다, 사람 지시): 제품 매뉴얼에 **"플러그인 설치" 장**을 더한다(9장 → 10장, 4.5 §2.10). Claude Code는 `/plugin marketplace add` → `/plugin install nerv@nerv-internal` → 재시작이고, Codex는 플러그인 형식이 없어 `.codex/config.toml`·`AGENTS.md` **초안 2종을 쓰는 저장소로 복사**하는 수동 경로다 — 둘 다 [4.6 플러그인](04-mvp/plugin.md) §4·§5를 인용하며 새 값을 만들지 않았다. **개념과 절차를 나눴다**: "에이전트 연동"은 무엇이 왜 그렇게 맞물리는지, "플러그인 설치"는 5단계 절차와 증상별 진단표(`NERV_UNAUTHENTICATED`·스코프 부족·`X-NERV-Project` 오타·A3 승인·`retry_after_s`·`stale` 회수). Codex의 경계도 숨기지 않고 적었다 — 도구는 전부 되지만 **훅 텔레메트리가 없어 세션 화면 해상도가 낮다**.
>
> v1.29 변경(2026-08-24 — 도움말과 제품 매뉴얼, 사람 지시): 전역 헤더에 **도움말 진입점**(`?` 한 칸)을 두고 그 아래 **제품 매뉴얼 9장**을 연다(4.5 §2.10 · REQ-WEB-071~074). 매뉴얼은 이 문서 세트와 **다른 문서**다 — 명세는 무엇을 왜 만드는지의 기록이고 매뉴얼은 만들어진 것을 어떻게 쓰는지의 안내다. 그래서 정본을 `codebase/apps/web/src/content/manual/` 에 두고 `docs/` 에 사본을 만들지 않는다(사본을 두는 순간 어느 쪽이 정본인지 다시 정해야 한다). **ko·en 두 벌**이고, "이 화면 도움말"은 짚어 줄 장이 없으면 항목 자체를 보이지 않는다. **갱신 규약을 AGENTS.md 구현 규약 5로 못박았다** — 화면·명령·계약이 바뀌면 매뉴얼도 같은 커밋에서 고친다. 곁가지로 헤더 드롭다운의 **항목이 눌리지 않던 결함**을 찾았다: 바깥 클릭 판정이 사용자 메뉴 ref 하나여서 조직·프로젝트 드롭다운 **안**의 클릭도 "바깥"으로 읽혔고, mousedown 에서 팝오버가 사라지면 뒤이은 click 은 이미 없는 요소로 간다 — 보이지만 눌리지 않는 상태였다.
>
> v1.28 변경(2026-08-24 — 링크를 잇고, 조직·프로젝트를 다룰 자리를 만든다): ① **EP-IMP-04 의 `pending` 을 실제로 구현했다**(4.7 §2.6e · 4.4). 계약에는 있었고 CLI 는 늘 빈 배열을 보냈으며 서버에는 그 절이 아예 없어서, 커버리지의 "요구사항 → 작업" 축이 언제나 0 이었다 — 화면은 정직하게 0 을 그렸지만 그 0 은 사실이 아니라 **묻지 않은 것**이었다. plan frontmatter 의 스펙 경로로 기준 SpecVersion 273건, 본문이 요구사항을 **하나만** 언급하는 경우로 링크 49건. 여럿이면 링크하지 않고 리포트에 올린다 ② **S8 조직·프로젝트 탭 신설**(4.5 · REQ-WEB-070). 헤더의 select 두 개가 고르는 것을 여기서 만들고 고치고 치운다. **프로젝트는 보관, 조직은 빈 것만 삭제** — 되돌릴 수 없는 일 앞에 되돌릴 수 있는 단계를 세운다 ③ 곁가지 둘: 셸이 전역 화면에서도 `useProject('')` 를 불러 **화면마다 조용한 500 두 개**가 깔려 있었고(어제 넣은 원인 사슬 로그 덕에 바로 보였다), 조직 권한 판정이 멤버십 **한 행**만 봐서 조직 admin 인데 프로젝트에서 planner 인 사람이 설정에서 잠겼다(겸직은 합집합이다 — 0003 의 규칙이 화면에서 다시 깨진 자리).
>
> v1.27 변경(2026-08-24 — 보관 토글이 죽어 있던 원인): 사람 보고 "보관 보기가 동작하지 않는다"를 좇아 보니 화면이 아니라 **데이터**였다. 임포터가 `done_at` 을 적재 시각으로 채워 완료 419건이 전부 "오늘 끝난 것"이 됐고, 그래서 완료 창(7일) 밖이 **0건**이었다 — 토글은 정확히 아무것도 없는 것을 걸러내고 있었다(4.7 §2.6d). git 에서 완료 시각을 되찾아 창 밖이 398건이 됐다. 함정은 **rename 감지가 pathspec 에 따라 달라진다**는 것이었다: `plan/` 을 주면 in-progress → complete 이동이 rename 이라 `--diff-filter=A` 에서 빠지고, 파일 하나만 주면 같은 파일이 잡힌다(409건 중 267건이 그렇게 사라졌다). 곁가지로 **시드 조직명을 `default` 로** 바꿨고(헤더에서 로고와 조직명이 둘 다 'NERV' 로 겹쳐 읽혔다), 그 과정에서 시드 안전장치가 slug 를 하드코딩해 **자기 시드의 조직을 남의 것으로 보고 재적재를 막는** 것을 찾아 상수 하나로 합쳤다.
>
> v1.26 변경(2026-08-24 — 헤더에 스코프 두 축): **프로젝트를 고를 길이 화면에 없었다**(사람 지적). 사이드바가 현재 프로젝트의 이름만 적어 두어, 옮겨 가려면 퀵 스위처(⌘K)를 알아야 했다 — 단축키를 아는 사람만 쓸 수 있는 길은 길이 아니다. 조직 select 오른쪽에 프로젝트 select 를, `홈` 오른쪽에 그 프로젝트로 가는 링크를 뒀다(4.5 §1.3 · REQ-WEB-069). 전역 화면(홈·승인함·알림)에는 라우트에 프로젝트가 없어 **마지막으로 본 프로젝트**를 보인다 — 비워 두면 "선택할 수 없다"로 읽힌다. 선택지가 하나여도 select 형태를 지킨다(사람 판단 — 예외 케이스가 없는 쪽이 직관적).
>
> v1.25 변경(2026-08-24 — 시안 전면 정합, 사람 지시): 아티팩트 시안과 구현의 격차를 4.5 v0.19 로 걷었다. 핵심은 컴포넌트가 아니라 **밀도**였다 — 시안의 타이포는 10.5/11.5/12.5/13.5px 반 픽셀 램프인데 구현은 11~14px 정수 램프였고, 네 화면을 나란히 놓으면 구현 쪽이 한 뼘 굵고 성글게 읽혔다. 램프를 시안 값으로 바꾸니 열세 화면이 함께 조밀해졌다. 구조 변화 셋: S1 홈(결정 수를 말하는 인사말 + 2단), S5 세션(master-detail — 고른 세션의 활동이 오른쪽 레일에), S3 문서 머리(메타 → 33px 제목 → 곁줄). S4 는 레인 접기 + 카드 상한(진행 중 24장이 보드를 세로 2,600px 로 만들고 있었다). IA 재배치는 하지 않았다 — 승인함·알림의 전역 헤더 유지(v1.20)와 막힘 레인 맨 앞(v1.17)은 사람이 정한 것이다.
>
> v1.24 변경(2026-08-24 — 적재 후 확인된 두 가지): ① **빈 스펙은 사라진 것이 아니라 묶음 노드였다**(4.5 REQ-WEB-068). 임포터는 디렉터리마다 `area` 노드를 만드는데 원본에 `_product-overview.md` 가 없으면 본문이 없다(clemvion area 16개 중 9개) — 데이터는 온전한데 화면이 빈 채로 두어 "내용이 사라졌다"로 읽혔다. 이제 무엇이고 왜 비었는지 적는다 ② **로그가 원인을 삼키고 있었다**. drizzle 은 드라이버 오류를 감싸면서 `message` 에 **SQL 전문**을 넣고 진짜 이유는 `cause` 에 둔다. 필터가 `stack` 만 찍어 로그에는 쿼리만 남았고, 그 쿼리는 psql 에서 멀쩡히 돌아 아무것도 알 수 없었다 — 원인 사슬을 끝까지 펴서 남긴다. 해당 500 자체는 컨테이너 재빌드 중의 일회성이었고 같은 요청은 지금 정상이다(`open_critical_findings: 422`).
>
> v1.23 변경(2026-08-24 — clemvion 리뷰 소급 적재): 리뷰 임포터(`nerv import review` · EP-IMP-06)를 구현하고 **spec·plan·review 를 전부 다시 적재**했다(4.7 §2.7). 결과: 스펙 141 · Task 484 · 관계 1,255 · 요구사항 739 · **리뷰 세션 1,984 · 리포트 13,826 · 발견 18,650**. ① **커밋 SHA 를 git 에서 되찾는다** — 옛 계획은 "`meta.json` 에 없으므로 NULL + 플래그"였는데 스키마가 NOT NULL 로 확정됐고, 리뷰 산출물 자체가 커밋돼 있어 되찾을 수 있었다(1,984건 전부·건너뜀 0) ② 원본이 **kind 마다 다른 모양**이라는 것을 두 번 배웠다: `meta.json` 의 자리(`files[]` vs `target_path`)를 놓쳐 consistency 922건이 461건으로 뭉쳤고, 표의 열 이름과 **이스케이프되지 않은 코드 스팬 안의 `|`** 때문에 백틱 한 글자가 제목이 되어 배치가 멈췄다 ③ 옮기는 것은 결론뿐이다 — 역할별 md 13,777개(131MB)·`_prompts/` 는 읽지 않는다(D-01·D-07) ④ **실측 규모가 화면 결함을 드러냈다**: 발견 18,650·브랜치 441 을 그대로 그리니 S6 이 30,396px 가 됐다. 상한을 넣고 **잘린 사실을 숫자로 말하게** 했다(REQ-WEB-067). 시드 두 건으로는 보이지 않던 것이다.
>
> v1.22 변경(2026-08-23 — FR-09 의 나머지 절반: 화면과 스킬): 서버만 있고 볼 곳이 없던 리뷰 데이터에 **S6 리뷰 센터**(4.5 §2.6a)와 **`/nerv:review` 스킬**(4.6 §2.6)이 붙었다. ① **facet 을 같은 응답에 싣는다**(EP-REV-03 확장) — 필터 칸의 숫자를 따로 받으면 그 사이에 새 발견이 들어와 "3건이라더니 4건"이 된다. facet 의 뜻은 "이것을 켜면 몇 건인가"라 각 차원은 **자기 선택을 빼고** 센다 ② **게이트 현황은 표시일 뿐 집행이 아니다**(EP-REV-04) — 막기 시작하면 판정이 틀렸을 때 사람의 일이 멈추고, 그때는 판정을 고칠 여유가 없다 ③ 와이어프레임과 다른 세 곳을 이유와 함께 남겼다: **표시 키를 만들지 않고**(`FND-4a19` 는 예시 표기이고 타입 문자를 늘리는 것은 새 결정이다 — 대신 "짧은 id"라고 **밝히고** 앞 8자를 준다), 리뷰가 없는 브랜치의 면제는 담지 못하며(결재가 브랜치를 가리킬 자리가 없다 — 스키마 결정이라 사람 확인 전까지 하지 않는다), `spec_drift` → CR 라우팅은 보낼 곳이 아직 없다 ④ 스킬은 **재시도하지 않는 규약**을 배포한다: critical 하향이 승인 큐로 가면 `approval_id` 와 함께 사람에게 보고하고 멈춘다(REQ-PLG-014·015). **MVP 약속은 전부 그대로다** — 도구 16종, 스킬 5종.
>
> v1.21 변경(2026-08-23 — **Phase 2 리뷰 수집(FR-09) 착수**, 사람 지시): 리뷰를 **파일이 아니라 레코드로** 받는 서버가 들어왔다(4.1 §5 착수 기록 · 4.4 §2.6a). ① `ReviewSession → ReviewerReport → Finding → FindingOccurrence → Resolution` 5층 적재 — 세션은 **changeset 하나에 하나**라 같은 커밋을 본 리뷰어 여럿이 한 세션의 리포트 여럿이 되고, 라운드는 서버가 센다 ② **fingerprint dedup**(줄 번호도 severity 도 넣지 않는다 — 줄이 밀렸다고 새 발견이 되면 dedup 이 무의미하고, severity 를 넣으면 리뷰어가 낮추는 순간 새 발견이 생겨 **하향이 감춰진다**. 원값은 `raw_severity` 에 남고 그 대조가 감사의 근거다) ③ **critical 하향은 A3** — 에이전트가 자기 리뷰의 심각도를 스스로 낮추면 게이트가 형식이 된다(clemvion 실측 732건 중 24건). 승인 카드를 만들고 막는데, **게이트는 트랜잭션 밖이어야 했다**: 같은 트랜잭션에서 막으면 그 카드도 함께 롤백되어 승인함에는 아무것도 뜨지 않는다 ④ MCP 도구 2종(P2) + REST 3종 — **MVP 는 여전히 16종**이고 카탈로그가 18종이다. 곁가지로 두 곳을 정정했다: 4.1 §5 의 낡은 행(전역 관계 그래프 뷰 — 이미 MVP), 그리고 **카탈로그 정본(3.4 §2.3)에 `nerv_spec_relate` 행이 없었다** — 2026-08-23 v1.9 에서 15종 → 16종을 확정하며 4부 문서들만 고치고 정본 표를 빠뜨린 것이다. 행을 넣고 "카탈로그 17종"을 **18종**으로 쓸었다(md·html 28곳). 그리고 **drizzle 스냅샷 체인이 깨져 있었다** — `0003_multi_role` 이 손으로 쓰인 마이그레이션이라 스냅샷이 0002 의 복사본이었고, `pnpm db:generate` 가 collision 으로 죽어 CI 의 "생성물 정합" 잡이 **이미 빨간 상태**였다(4.3 v0.9). 체인을 다시 세웠다.
>
> v1.20 변경(2026-08-23 — 앱 셸을 시안에 맞춘다): 시안과 구현의 남은 차이가 **셸**이었다 — 재구성에서 `app-shell` 을 한 번도 건드리지 않아 열세 화면이 옛 틀 안에 앉아 있었다(4.5 §1.3). 로고를 사각형 마크로, 프로젝트에 색 배지, nav 29px + 세션 활성 건수 배지, 사이드바 바탕을 옅게. **IA 는 정본을 지킨다** — 시안은 승인함을 프로젝트 사이드바로 내렸지만 승인함·알림은 조직 전역이라 프로젝트가 여럿일 때 자리가 틀린다.
>
> v1.19 변경(2026-08-23 — 이동 시 본문 미갱신 · 전 화면 점검): ① **스펙을 옮겨도 본문이 앞 문서였다**(4.5 §2.4e · REQ-WEB-060) — 라우트 파라미터만 바뀌면 컴포넌트가 재사용돼 `draft` 가 살아남는다. 보이는 것보다 나쁜 것은 저장이다: 남의 본문을 덮어쓴다. **E2E 여야 잡힌다** — jsdom 테스트는 수정을 빼도 통과했다 ② 열세 화면 제목이 20px/600 인데 스펙 상세만 26px/700 이었다. `PageHeader` 한 곳에서 27px 시안 값으로 통일 ③ 멤버 표 역할 칸이 `w-32` 라 칩 여섯이 네 줄로 접혀 행이 117px 였다(→ 39px) ④ 시드의 `blocked_reason` 이 `awaiting_answer` 라는 **코드값**이라 화면에 그대로 찍혔다.
>
> v1.18 변경(2026-08-23 — 시안 대조 정정): 구현이 시안과 갈라진 네 곳을 되돌렸다(4.5 §2.5). ① **레인 배경 제거** — 가라앉은 상자를 레인마다 세워 카드에서 걷어낸 테두리가 레인 단위로 되살아나 있었다 ② 레인 머리를 배지에서 **맨 점 + 이름 + 흐린 건수**로 ③ 카드 **경과 시간을 오른쪽 끝**에 — 세로로 훑히는 값은 열이 맞아야 읽힌다 ④ 요약 스트립에 **내 담당** 추가. 시안이 정한 것을 구현이 임의로 바꾸면 시안을 본 뜻이 없다.
>
> v1.17 변경(2026-08-23 — 재구성 후 파손 3건 정정): ① **대시보드 세션 줄 붕괴** — 2열 격자(350px)에 1440px 기준 고정 폭 줄을 넣어 가운데 칸이 한 글자 폭으로 찌그러졌다. `@container` 로 자기 폭을 보게 고쳤다(REQ-WEB-058) ② **스펙 곁레일이 길면 아래에 닿지 못했다** — `sticky` 만으로는 부족하다. 레일 안에서 스크롤한다(REQ-WEB-059 · 실측 3358px 내용 / 856px 창) ③ **막힘 레인을 하단 접이식 → 가로줄 맨 앞**으로(4.5 §2.5 정본 개정, 사람 판단) — 아래에 두든 흐름 끝에 두든 "있긴 한데 안 보인다"였다.
>
> v1.16 변경(2026-08-23 — 시각 위계 재검토): 화면이 텍스트 나열로 읽힌 원인을 짚고 네 화면(작업 보드·스펙 상세·세션 모니터·홈)을 다시 그렸다(4.5 §2.4d). 색이 아니라 **정보가 다 같은 무게로 있는 것**이 문제였다 — ① 위계를 세 단으로 벌리고(`SummaryStrip`·`Avatar`·`SectionLabel` 신설) ② 카드 테두리를 걷고 주의가 필요한 것만 좌측 룰로 튀게 하고 ③ 사이드바 141줄의 `승인됨` 배지 반복을 없앴다(다 같으면 신호가 아니다). 문서 본문은 44rem 폭 · 15px 로 UI 글자와 분리. REQ-WEB-056·057. 상태 토큰 8종과 §4.1·§4.2 매핑은 불변.
>
> v1.15 변경(2026-08-23 — 문구 하드코딩 lint 강제): REQ-CB-022 가 관례로만 지켜지던 것을 규칙으로 세웠다(4.2 §4.2a). 표면 코드에서 카탈로그를 거치지 않은 문구 **50건**을 찾아 25건을 카탈로그로 옮기고 25건(운영자용 로그·설정 오류·내부 불변식)에 사유와 함께 예외를 달았다. 템플릿 리터럴은 보지 않는다 — `sql` 주석 오탐이 신호를 덮어서다(1218 → 50). **MCP 인자 설명도 로케일**을 타게 했다(REQ-CB-024).
>
> v1.14 변경(2026-08-23 — 겸직 RBAC): 한 사람이 한 스코프에서 **역할을 여럿** 가진다(4.3 REQ-DB-010 개정 · `0003_multi_role`). 근거는 실측 — clemvion `owner:` 라벨의 복합 표기 20건. ① **역할 매트릭스 신설**(4.4 §1.6a) — `assertScope` 가 "세션은 역할 매트릭스가 판정한다"고 적어 두고 그 매트릭스가 없어 웹으로 들어오면 `viewer` 도 메타 편집·아카이브·베이스라인 동결을 통과했다 ② 멤버십을 **행 여럿**으로(배열이 아니라 — 부여 이력이 남는다), 판정을 **합집합**으로(코드 곳곳의 "admin 우선 1건" 정렬이 겸직에서 권한을 조용히 지웠다) ③ 멤버 탭 역할 칩 토글 · 시드에 겸직 한 벌. ④ **EP-SPEC-07 타입 제한을 실제로 연결**(4.4 §1.6a) — 함수만 있고 엔드포인트에 물리지 않은 채였다. qa 는 스펙을 만들지 않는다(리뷰가 그 몫이고 Phase 2다 — 사람 확인 2026-08-23). D-06(지시자≠승인자)은 사람 기준이라 불변.
>
> v1.13 변경(2026-08-23 — 정본 전수 대조로 찾은 결함 4건): REQ 164건을 코드와 대조했다. ① **에이전트가 스펙을 새로 만들 수 없었다** — MCP `nerv_spec_draft_upsert` 스키마에 `key`·`title`·`type`·`parent_id` 가 없어 이어쓰기만 됐다(4.4 §2.2). 함께 REQ-API-021(`meta_change_not_allowed` 409)도 구현 ② **Codex 초안 2종이 없었다**(REQ-PLG-010) — 플러그인이 `plugin/codex/` 로 템플릿을 배포한다(4.6 §5.3) ③ 인증 실패 사유가 응답에 실려 "형식 오류/모르는 토큰"을 구별시켰다 — 코드 주석이 막으려던 바로 그것 ④ clemvion 프로파일 기대 집계가 낡아 내장 프로파일로는 임포트가 중단됐다(135/117 → 136/93, 실측 시점 명기).
>
> v1.12 변경(2026-08-23 — 작업 보드 레인·보관 창): **정본이 이미 `done(7d)`·"backlog 는 필터"를 말하고 있었고 코드가 그것을 구현하지 않았다**(4.5 §2.5). clemvion 실측 487건 중 done 이 419건(86%)이고 보드가 전량을 한 응답(229 KB)으로 받고 있었다. ① EP-TASK-01 을 계약대로 `Page<>`+커서로 (구현이 맨 배열이었다 — 4.4 §1.6) ② 레인 5종 + backlog 필터 + blocked 하단 접이식 ③ **보관 보기** 토글 — 아카이브를 **상태값이 아니라 `done_at` 파생 창**으로 둔다. 종단 상태가 늘면 "왜 끝났는가"를 잃기 때문이다. REQ-WEB-054·055. 실측 1요청 229 KB → 6요청 1 KB.
>
> v1.11 변경(2026-08-23 — 시드가 Activity 타임라인을 심는다): 시드가 `activity` 를 비워 두어 **S5 세션 상세의 타임라인이 개발 환경에서 늘 빈 목록**이었다(4.3 §4). 그 화면의 값어치가 "사람의 개입이 에이전트의 행동과 같은 줄에 섞여 보이는 것"인데 중심이 비어 있으면 만들다 만 껍데기로 보인다. 세 세션에 5종 어휘를 전부 심고, 개입은 앞뒤로 action 이 있는 자리에 둔다. "화면마다 데이터 있음 경로가 뜬다" 검사에 세션 상세가 빠져 있어 지나갔던 것이라 그 자리에 넣었다.
>
> v1.10 변경(2026-08-23 — 개발 루프 구성요소 분리): **`pnpm dev:api`·`dev:web`·`dev:worker` 신설**(4.2 §5.1) — 워크스페이스의 `dev` 를 직접 부르면 빌드 감시가 없어 소스를 고쳐도 아무 일이 안 일어나므로, 별칭이 아니라 `dev.mjs --only` 선택으로 만들었다. `dev:worker` 는 "전부 + 워커" → **"워커만"**으로 뜻이 바뀌고 예전 뜻은 `dev:all`. `dev:web` 은 `.env` 를 요구하지 않는다. 곁가지로 **본 배럴에 `node:crypto` 가 새어 웹이 로드에서 터지던 것**을 `@nerv/schema/keys` 서브패스로 분리하고, 같은 결함이 세 번째로 나지 않도록 배럴 브라우저 안전성 검사를 신설했다.
>
> v1.9 변경(2026-08-23 — 트리 조작·표·도구 16종 확정): ⓪ **MCP 도구 15종 → 16종 확정**(사람 확인 2026-08-23 — `nerv_spec_relate` 신설, 4.1 §4.2). 수가 6개 문서에 흩어져 있어 4.1·4.2·4.4·4.6·4.7·4.8을 함께 정정했다 ⓪′ **임포트 계약에 `sort_key` 추가**(4.7 §2.2 · REQ-IMP-019 · 4.4 EP-IMP-02) — 매핑 규칙은 원래 문서에 있었는데 계약에 필드가 없어 임포트를 넘지 못했다. clemvion 실측으로 `2-navigation` 아래 `0-dashboard` 가 세 번째, `10-auth-flow` 가 첫 번째에 있었다. 기존 노드에는 다시 쓰지 않는다(메타는 EP-SPEC-15 소관) ⓪″ **임포터 결함 2건**(4.7 §2.6b·2.6c · REQ-IMP-020·021) — ① Task 표시 ID 가 16진 4자(65,536)라 clemvion plan 481건에서 **1건을 조용히 먹었다**(충돌 기댓값 1.76, 실측). 폭을 12자로 넓히고 알고리즘을 계약 패키지로 옮겼다. 형식도 §5.1 정본(`CLV-T-1KTDCK`)으로 맞췄다 — **생성기가 둘이었고** 일반 Task 생성 경로는 아직 4자였다(`task_key_uq` 덕에 조용하진 않지만 484건 시점 충돌 확률 0.74%). ② plan 패스만 배치 분할이 빠져 계약 상한 200을 넘겼다 ③ **nginx `/api/` 에 `client_max_body_size` 가 없어** nginx 기본 1 MiB 가 걸렸다(서버 선언은 16 MiB) — 임포트 배치 1.39 MB 가 413. 앞문이 서버보다 좁으면 서버의 상한은 선언일 뿐이다. k8s Ingress 도 같이 정정(4.2 배포 산출물) ① **트리 접힘 결함 정정**(4.5 §2.4b) — `isOpen` 에 `depth === 0` 이 OR 로 걸려 최상위가 접히지 않았다. 트리의 **깊이 제한은 원래 없었다**(API 는 전 계층을 주고 clemvion 은 4단계) ② 자식 수·조상 자동 펼침·펼침 상태 보존·전체 펼치기 추가(REQ-WEB-052·053) ③ **§2.4c 스펙 표 신설** — "어디가 비었나"에 답하는 정렬 가능한 열. 다른 결정·요구는 불변.
>
> v1.8 변경(2026-08-23 — 관계 그래프): ① **관계 자동 추출을 임포터로 확장**(4.7 §2.4) — 서버는 원본 체크아웃을 못 보므로 상대경로 링크(`[텍스트](../x.md)`)는 클라이언트가 키로 바꿔 보낸다. clemvion 간선 0 → 1,253 ② **EP-SPEC-19 전역 그래프**(4.4 §2.2) — 노드·간선을 한 응답으로 ③ **§2.4a 관계 그래프 화면 신설**(4.5) — 트리/그래프 두 탭, 밀도 대응 3종. 렌더링 **Cytoscape.js + fcose 스택 추가**(4.1 §2.1) ④ **MCP 도구 15종 → 16종**(4.1 §4.2 — `nerv_spec_relate` 신설). `refines`·`depends_on` 은 문서를 읽어야 아는 판단이라 에이전트가 채운다; 읽기는 `nerv_spec_tree` 옵션으로 접어 새 도구를 만들지 않았다. 신설 요구 REQ-WEB-050·051.
>
> v1.7 변경(2026-08-23 — clemvion 이관 실행): ① **`tree.area_from_directory` 구현**(4.7 §2.2) — 디렉터리마다 area 노드를 만들고 `_product-overview.md` 가 그 노드의 본문이 된다. 없으면 본문 없는 노드 + 리포트 표기. 이것이 없어 130편이 최상위에 평탄하게 붙어 있었다 ② **키·경로 규칙 명시**(4.7 §2.2) — `tree.overrides` 는 스캔 뿌리 기준 상대 경로, frontmatter `id` 없으면 경로에서 키를 만든다(파일명 폴백은 동명 파일끼리 조용히 덮어썼다) ③ **시드에 관리자 계정 분리**(4.3 §4 — `admin@example.com`, 조직 스코프). 기존 다섯은 온보딩용 인물이고 조직을 세우는 계정은 따로 있어야 한다 — 이 계정이 없어 임포트·게이트 정책·멤버 역할을 시드만으로 시연할 수 없었다. 다른 결정·요구는 불변.
>
> v1.6 변경(2026-08-23 — 개발 루프 실행에서 드러난 결함 3건): ① **`@nerv/schema` 배럴에서 마이그레이터·시드 분리**(4.2 §3.1 — `@nerv/schema/migrate` 서브패스). 배럴이 `pg` 를 재수출해 **브라우저가 Postgres 드라이버를 평가하다 죽었다**(웹이 백지). ② **`NERV_TRUSTED_ORIGINS` 신설**(4.2 §5.2) — 개발 루프는 화면(Vite :5173)과 API(:8080)의 오리진이 달라 better-auth 가 로그인을 `INVALID_ORIGIN` 으로 막았다. ③ **실시간 소켓은 인증된 뒤에만 붙는다** — 로그인 화면에서 붙었다가 거절당한 소켓은 socket.io 가 자동 재연결하지 않아, 로그인 후에도 실시간이 죽은 채 남았다(제품 결함이며 dev 전용이 아니다). 셋 다 프로덕션 조립에서는 가려져 있었다. 다른 결정·요구는 불변.
>
> v1.5 변경(2026-08-23 — 개발 루프 정정): **`pnpm dev` 를 런처로 묶는다**(4.2 §5.1 — `codebase/scripts/dev.mjs`). 워크스페이스별 `dev` 스크립트를 `--parallel -r` 로 늘어놓는 방식이 두 군데서 어긋났다: ① 로컬 프로세스에 `.env` 를 넣어 주는 주체가 없어 API 가 `DATABASE_URL 이 없습니다` 로 즉시 죽고 ② `tsc -b --watch` 가 소비자를 따라 내려가지 않아 **소스를 고쳐도 반영되지 않았다**. 둘 다 실측이다. 다른 결정·요구는 불변.
>
> v1.4 변경(2026-08-23 — 다중 세션 대응): **E2E 스택의 프로젝트 이름·포트를 세션마다 할당**한다(4.2 §4.3). 한 기계에서 여러 세션이 동시에 E2E를 돌리면 고정 이름은 남의 컨테이너를 재사용하고 고정 포트는 기동에 실패한다 — 둘 다 조용히 남의 테스트를 깨뜨린다. E2E 전용 포트 대역 `19000~19999`를 신설해 개발 스택의 고정 포트(8090 `embed` 포함)와의 충돌도 함께 없앤다. 할당기는 `codebase/scripts/e2e-stack.mjs`. 다른 결정·요구는 불변.
>
> v1.3 변경(2026-08-23 — Apple Silicon 호환): **로컬 임베딩 프로필의 이미지를 TEI → ollama 로 교체**(4.2 §5.2a). TEI 는 arm64 이미지를 발행하지 않아 Apple Silicon 에서 `no matching manifest` 로 기동되지 않고, amd64 에뮬레이션은 워밍업에서 OOM 으로 죽는다(실측). ollama 는 amd64·arm64 둘 다 발행하고 같은 bge-m3(F16 · 1024차원)를 같은 OpenAI 호환 계약으로 노출한다 — **계약·모델·차원·외부 전송 0 이 전부 그대로**이므로 스택 결정의 변경이 아니라 프로필 실물의 교체다(REQ-CB-020 의 제공자 무관 설계가 이것을 값싸게 만든다). 다른 결정·요구는 불변.
>
> v1.2 변경(2026-08-23 — i18n 적용): **문구 카탈로그와 로케일**을 신설한다(4.2 §3.4가 정본). 웹·API·CLI 세 표면이 `@nerv/schema`의 한 벌을 쓰고, 지원 로케일은 **한국어(기본) + 영어**다. 표면별 로케일 출처: 웹은 사용자 선택 → 브라우저, API·MCP는 `Accept-Language`(4.4 §1.4), CLI는 `NERV_LANG`/`LANG`(4.7 §3.5a). 번역하지 않는 것 셋을 함께 못박는다 — **식별자**(`spec.approved`·`ready`)·운영자용 로그·DB에 저장되는 콘텐츠. 화면의 언어 전환 UI는 4.5 §1.7. 신설 요구 REQ-CB-022~024 · REQ-API-030 · REQ-WEB-048·049 · REQ-IMP-018. 상태 배지가 식별자 대신 **번역된 라벨**을 보이도록 4.5 §4.3을 정정했다. 다른 결정·요구는 불변.
>
> v1.1 변경(2026-08-23 — 구현된 UI를 보고 정정): **디자인 토큰 §4.4 신설**(4.5 §4.4 — 표면·타이포·간격·라운드). v1.0까지 §4는 색만 정했고 나머지는 화면마다 손으로 적혀 값이 갈라져 있었다. 함께 기록한 것 셋: ① **중립 램프를 따뜻한 회색으로 재조정**(상태 토큰 8종의 색값·의미·매핑은 불변 — §4.1·§4.2 그대로) ② 다크 팔레트를 `@media` 안의 `@theme` 으로 쓰면 Tailwind v4가 조건을 지워 **앱이 항상 다크였던** 결함의 점화 기록 ③ 정의 없이 이름만 쓰이던 `prose-nerv`(스펙 본문 스타일) 규약. 신설 요구 REQ-WEB-045~047. 다른 결정·요구는 불변.
>
> v1.0 변경(2026-08-22 — 구현 착수 중 발견): **인증 인프라 테이블 3종 명세 추가**(4.3 §2.16 — `auth_session`·`auth_account`·`auth_verification` + `"user"` 2컬럼). 확정 스택 better-auth가 요구하는 물리 테이블인데 DDL 전문에 빠져 있어 웹 세션 인증 구현이 막혀 있었다. **엔티티 29종 카운트는 불변**(검색 인덱스와 같은 "도메인 엔티티 아님" 등급). 같은 절에서 **organization 플러그인 미사용**을 확정한다 — 조직·멤버십·역할의 정본은 도메인 테이블(3.3 §2.1)이고 플러그인은 같은 사실을 두 곳에 저장하게 만든다. 다른 결정·요구는 불변.
>
> v0.9 변경(2026-08-22 — 구현 착수 중 발생): **배포 산출물 위치 개정(REQ-CB-015)** — 배포 트리를 `codebase/deploy/`에서 **저장소 루트 `deploy/`**로 옮기고, 저장소 구역을 문서·규약·코드(`codebase/`)·배포(`deploy/`) 넷으로 정리했다. 이미지 빌드 컨텍스트는 저장소 루트로 통일(4.2 §5.3·§6.1). 다른 결정·요구는 불변.
>
> v0.2 변경: ① 구현 코드 위치를 저장소 `codebase/` 하위로 확정(4.2 §1, REQ-CB-015) ② 실시간 채널을 WebSocket 단일에서 **WebSocket + SSE 다중 채널**로 확장하고 팬아웃 **방송 MQ를 Valkey pub/sub**로 확정(4.1 §2 · 4.4 §3 · 4.3 §3) ③ 에이전트 작업 규약 [AGENTS.md](../AGENTS.md) 신설(CLAUDE.md가 import).
>
> v0.8 변경(2026-08-22 — 재검토 후속): ① 검색 p95 목표치 **보류 결정** 기록(E06-S06 실측 후 확정 — 4.4 §2.2b) ② `nerv_spec_search`에 `references?` 필터 추가(REST와 입력 정합 — 3.4 §2.3) ③ **로드맵 v0.2** — Phase 0·1 범위 표에 하이브리드 검색·탐색 UI 역반영(판정·배분 불변, 표기 보완).
>
> v0.7 변경(2026-08-22 — 임베딩 제공자 추상화, **v0.6 "자가호스팅만"의 당일 번복**): 임베딩 호출을 **OpenAI 호환 `/v1/embeddings` 단일 계약**으로 추상화 — 제공자는 env 프로필(로컬 = TEI CPU / 스테이징 = LM Studio / **운영 = OpenAI** `dimensions=1024`). 외부 전송 금지 조항(구 REQ-CB-020)은 폐기하고 외부 전송은 운영 주체의 env 명시 선택으로 전환(기본값 자가호스팅, 고기밀 프로젝트는 자가호스팅 권고). 차원 1024 전 프로필 고정(REQ-CB-021), 프로필 정본 4.2 §5.2a.
>
> v0.6 변경(2026-08-22 — 스펙 열람·검색 대규모 대응): ① **하이브리드 검색 MVP 확정** — Postgres FTS + pg_trgm + **pgvector**(HNSW) + **자가호스팅 임베딩**(`nerv-embed` — TEI + BGE-m3, 외부 임베딩 API 금지) + **관계 확장(graph RAG)**. 근거: `nerv_spec_search`가 P0 도구 — 에이전트가 첫날부터 검색의 주 소비자(4.1 §2.1 · 4.4 §2.2b · 4.3 §2.15) ② **관계 가시화** — EP-SPEC-18(역참조 1급)·S3 관계 패널·영향 미리보기 ③ **탐색 UI** — 전역 퀵 스위처(⌘K)·트리 지연 로드·검색 결과 뷰(4.5) ④ 인프라 서비스 4종(+embed), 백로그 스토리 68 → 74.
>
> v0.5 변경(2026-08-22 — 구현 착수 검토의 공백 보완): ① **스펙 메타 표면 신설**(EP-SPEC-15~17 — 이동·개명·아카이브. FR-01 "이동·개명에도 ID 불변"의 실행 경로, 4.4 §2.2 · 4.5 §2.4) ② **`gate_policy`·`retention` 키 스키마 확정**(4.4 §2.1a — S8 편집 항목 정합) ③ **쿼터 시작값**(4.4 §1.8 — PAT 300/min · 웹 600/min · ingest 120/min) ④ **`spec_relation` 자동 추출**(4.4 REQ-API-024 — 참조 전파의 데이터 전제) ⑤ **오프라인 폴백 실물**(4.6 §3.4 — `.nerv/cache`·`outbox`) ⑥ **테스트 러너 확정**(Vitest + Playwright — 4.1 §2.1) ⑦ **CI 전문**(4.2 §4.5) ⑧ **백업·복구 절차**(4.2 §6.5). 백로그 스토리 65 → 68.
>
> v0.4 변경(2026-08-22 — 임포터 실행 모델·범용화): ① 임포터를 **프로파일 기반 범용 스펙 임포터**로 확정하고(clemvion·nerv-docs는 내장 프로파일, 사용자 정의는 프로파일 파일) 문서 제목을 "4.7 스펙 임포터"로 변경 ② 실행 모델을 DB 직결에서 **API 클라이언트 CLI**로 전환 — 운영 서버가 원본 체크아웃에 접근할 수 없다는 사실이 근거(4.7 §3.2). 임포트 REST 표면 **EP-IMP-01~05** 신설(4.4 §2.10), 모노레포에 **`apps/cli`** 추가(4.2 §1.3) ③ 플러그인 **스킬 4종 → 5종**(`/nerv:import` — 4.6 §2.5), **MCP 도구는 15종 유지** ④ 백로그 스토리 62 → 65(E07-S04·S05 · E12-S05).
>
> v0.3 변경(FR-02 범위 확장 — 스펙이 구현보다 앞서갈 때의 기준 관리): ① **베이스라인**(프로젝트 승인 세트의 이름 있는 동결)을 MVP 핵심으로 포함 — 테이블 29종(+`spec_baseline`·`spec_baseline_item`), EP-SPEC-11~14, as-of manifest(3.5 §3.6 · 3.3 §2.2) ② **기준 버전 규약** — Task 컨텍스트는 파생 버전(`source_spec_version_id`)의 불변 스냅샷을 읽고, superseded 시 서버가 `basis_superseded` 표시 + 재브리핑 플래그(3.4 §2.4 · 3.5 §3.3) ③ **참조 문서 전파** — 승인 시 `spec_relation` 역방향 재검토 신호(`spec.recheck_requested`).

## 읽는 순서

처음이라면 **[html/index.html](html/index.html)** (개요)에서 시작해 1부→4부 순서로 읽는 것을 권장한다.
빠르게 결론만 보려면: [문제 정의](01-problem/pain-points.md) → [비전](03-proposal/vision.md) → [로드맵](03-proposal/roadmap.md).
구현에 바로 착수하려면 4부만 읽어도 되도록 쓰였다: [MVP 범위와 스택 확정](04-mvp/scope.md)부터 시작한다.

## 문서 목차

### 1부 · 현행 분석 (왜 필요한가)

| 문서 | 내용 |
| --- | --- |
| [1.1 clemvion 하네스 분석](01-problem/clemvion-analysis.md) | 현행 1인용 SDD+TDD 하네스의 구조(스킬·훅·워크플로우·spec/plan/review 체계) 전수 분석, 계승할 자산과 구조적 한계 — 전부 실측 근거 |
| [1.2 문제 정의와 요구사항](01-problem/pain-points.md) | 문제 P1~P8(스펙 충돌·중복 작업·버전 부재·추적 곤란·출처 단절·git 비대화·직군 장벽·n:n 부재)의 증상→근거→근본 원인, **FR-01~17 / NFR-01~05 요구사항 정의** |

### 2부 · 리서치 (근거)

| 문서 | 내용 |
| --- | --- |
| [2.1 Spec-Driven Development](02-research/spec-driven-development.md) | Spec Kit·Kiro·OpenSpec·Tessl·BMAD·spec-workflow-mcp 등 SDD 도구 생태계 분석 — 수렴하는 문서 구조와 **전 도구 공통의 멀티유저 공백**(1차 출처), 워터폴 비판·반론 |
| [2.2 병렬 에이전트 오케스트레이션](02-research/agent-orchestration.md) | Conductor·vibe-kanban·claude-squad·Claude Code web·Codex cloud·Copilot·Jules·Devin 등의 할당·격리·상태 표시·충돌 방지·개입 5축 비교, 검증된 패턴(원자적 클레임·세션 카드·3게이트), 리뷰 병목 실측 |
| [2.3 협업 플랫폼의 에이전트 통합](02-research/collab-platforms.md) | Linear for Agents(세션 상태머신·SLA·activity)·GitHub(Agent HQ·지시자≠승인자)·Jira Rovo·Notion·Asana·Slack — "에이전트=팀원" 모델의 수렴 패턴 |
| [2.4 Claude Code/Codex 연동 기술](02-research/integration-tech.md) | hooks 31종(`type:"http"`)·MCP(도구/리소스/OAuth·리비전 호환)·skills·plugins·OTel·Agent SDK, Codex 패리티(tools-only 제약) — NERV가 실제로 쓸 연동 표면 카탈로그 |

### 3부 · 플랫폼 제안 (무엇을 어떻게 만들 것인가)

| 문서 | 내용 |
| --- | --- |
| [3.1 비전과 핵심 시나리오](03-proposal/vision.md) | 한 줄 정의·3대 가치·포지셔닝("SDD 도구들의 Linear"), 직군별 페르소나 4종의 하루, 핵심 여정 3개, **Build vs Buy 비교**, 성공 지표 |
| [3.2 시스템 아키텍처](03-proposal/architecture.md) | 컴포넌트 구성(웹·API·MCP 게이트웨이·훅 수집기·DB·워커), **저장 전략(DB 단일 진실 + md 미러 + git export)**, 데이터 흐름 시퀀스 4종, 기술 스택 선정·대안 비교, 보안·확장 |
| [3.3 데이터 모델](03-proposal/data-model.md) | ERD 전체와 엔티티 상세(Spec/SpecVersion/Requirement, Task/Claim, AgentSession/Activity, ReviewSession/Finding, Approval/Question, Event…), clemvion frontmatter 매핑, 검증 질의 |
| [3.4 에이전트 연동 설계](03-proposal/agent-integration.md) | 3층 연동(MCP tools-first / 훅 텔레메트리 / 플러그인·AGENTS.md 배포), `nerv_*` MCP 도구 카탈로그, Claude Code·Codex 설정 예시, 세션 수명주기 규약, 보안 |
| [3.5 스펙 워크플로우와 거버넌스](03-proposal/spec-workflow.md) | 스펙 2축 상태(문서 승인 축 × 요구사항 구현 축), 승인·CR 흐름, Task 파생→클레임→게이트, 리뷰 파이프라인(fingerprint dedup·커버리지), 알림 설계 |
| [3.6 화면 설계](03-proposal/ui-wireframes.md) | IA와 S1~S8 와이어프레임(대시보드·프로젝트 개요·스펙 상세·작업 보드·세션 모니터·리뷰 센터·승인함·설정) — HTML 판은 실제 렌더링 목업 |
| [3.7 로드맵](03-proposal/roadmap.md) | Phase 0 PoC(조정 검증) → 1 MVP → 2 리뷰·연동 확장 → 3 고도화, 각 단계 성공 기준·리스크·clemvion 마이그레이션 계획 |

### 4부 · MVP 구체화 (구현 착수 가능한 명세)

| 문서 | 내용 |
| --- | --- |
| [4.1 MVP 범위와 스택 확정](04-mvp/scope.md) | MVP 가치 가설과 "구현 착수 가능" 정의, 확정 스택 전문(결정일·재검토 트리거), FR-01~17 포함/부분/제외 표, 화면·도구(16종)·스킬(5종) 범위와 non-goals |
| [4.2 코드베이스와 배포](04-mvp/codebase.md) | 저장소 구역(`docs/`·`codebase/`·`deploy/`)과 모노레포 트리 전문(`codebase/` 하위 — `apps/web`·`apps/api`·`apps/cli`·`packages/schema`), NestJS 모듈 맵(D-05 실물), 개발 환경 부트스트랩·docker-compose 전문, k8s(kustomize) 운영 배포 |
| [4.3 데이터베이스 스키마](04-mvp/database.md) | 29개 테이블 전체 DDL(FK·CHECK·인덱스·트리거·파티션), 이벤트 방송 규약(Valkey `nerv_events`), 개발 시드, 마이그레이션 왕복 수용 기준 — [3.3 데이터 모델](03-proposal/data-model.md)의 DDL 정본 |
| [4.4 API 명세](04-mvp/api.md) | `/api/v1` 공통 규약(인증 2경로·에러 코드·멱등키·페이지네이션), 리소스별 엔드포인트 전표, 실시간 채널 계약(WebSocket + SSE — 룸·이벤트), 임포트 표면(EP-IMP-01~05), MCP 16종 ↔ REST 대응 표 |
| [4.5 화면 명세](04-mvp/screens.md) | 라우팅 맵과 앱 셸, 화면별 데이터 소스·WS 구독·상태 3종·컴포넌트·수용 기준, TipTap 에디터 상세, 디자인 토큰. 와이어프레임 커버리지 표(§1.6) — S1~S8 그림은 [3.6 화면 설계](03-proposal/ui-wireframes.md), 신설 화면·하위 뷰(앱 셸·로그인·온보딩·알림 센터·스펙 목록·작업 상세 패널) 그림은 이 문서가 소유 |
| [4.6 플러그인과 온보딩](04-mvp/plugin.md) | 스킬 5종 SKILL.md 전문(`/nerv:next`·`/nerv:spec`·`/nerv:impl`·`/nerv:question`·`/nerv:import`), hooks.json·`.mcp.json` 전문, 사람 온보딩 절차(PAT 발급→설치→bootstrap), Codex 경계 |
| [4.7 스펙 임포터](04-mvp/importer.md) | 프로파일 기반 범용 임포터 — 내장 프로파일 `clemvion`(spec 135md·plan 450md)·`nerv-docs`, 파싱 규칙과 Spec/Requirement/Task 매핑, CLI(`nerv import`, dry-run 기본)+임포트 API 실행 모델, 래퍼 스킬 `/nerv:import`, 실패 리포트 형식과 수용 기준 |
| [4.8 백로그](04-mvp/backlog.md) | Phase 0·1 에픽/스토리 분해(`E01-S01` 형식, EARS 수용 기준·근거 링크), 의존 그래프와 착수 순서, E2E 수용 시나리오 |

## 핵심 수치 (전체 문서의 근거 뼈대)

- clemvion `review/`에만 **markdown 13,777개·131MB** — review 이력 blob이 `.git` packed blob 바이트의 **60%**
- **73일간 리뷰 세션 1,891개**(일평균 26개), 커밋 2,464개 중 38%가 review/ 접촉 — 리뷰 산출물이 다음 리뷰의 diff에 포함되는 **자기증식 루프** 실측
- 스펙 동시수정 자동 검출은 **"다른 머신·세션이면 로컬에서 안 보인다"는 이유로 의도적으로 제거**됨(clemvion #576) — 로컬 git 아키텍처의 구조적 상한
- 업계 실측: AI 도입 팀에서 **PR 리뷰 시간 중앙값 +441%, 무리뷰 머지 31%**(Faros AI, 2.2만 명 계측) — 병렬 에이전트의 제1 실패 모드는 리뷰 병목

## 이 문서 세트의 관리 규약 (유지보수용)

- **md가 원본**이고 html은 사람 열람용 파생본이다. 내용 수정 시 둘 다 갱신한다(구조는 `html/`의 사이드바 순서를 따른다).
- **애플리케이션·패키지 코드는 저장소 `codebase/` 하위에, 배포 산출물은 저장소 루트 `deploy/` 하위에** 둔다([4.2 코드베이스와 배포](04-mvp/codebase.md) §1, REQ-CB-015 — 2026-08-22 개정). `docs/`에는 문서와 html 파생본만 들어간다.
- **제품 매뉴얼은 이 문서 세트가 아니다.** 제품을 쓰는 사람을 위한 안내(`/help`)의 정본은 `codebase/apps/web/src/content/manual/<로케일>/<장>.md`이고 **ko·en 두 벌**이며, `docs/`에 사본을 두지 않는다 — 두는 순간 어느 쪽이 정본인지 다시 정해야 한다. 명세는 무엇을 왜 만드는지의 기록이고 매뉴얼은 만들어진 것을 어떻게 쓰는지의 안내다. 화면 명세는 [4.5 화면 명세](04-mvp/screens.md) §2.10, 갱신 규약은 [AGENTS.md](../AGENTS.md) "구현 작업 규약" 5.
- 에이전트(Claude Code·Codex) 작업 규약의 정본은 저장소 루트의 [AGENTS.md](../AGENTS.md)다. `CLAUDE.md`는 그것을 import만 한다 — 어느 머신·세션에서 작업해도 같은 규약이 적용되게 하기 위해서다.
- 용어·상태값·결정 번호(D-01~D-14)·요구사항 번호(FR/NFR)는 [pain-points.md](01-problem/pain-points.md)의 정의를 단일 기준으로 한다.
- 근거 URL은 실제 접속 확인된 것만 싣는다. clemvion 근거는 `clemvion:경로` 표기.
- 큰 방향 변경(결정 추가·폐기)은 이 README의 버전을 올리고 각 문서 머리의 버전 표기를 동기화한다.
