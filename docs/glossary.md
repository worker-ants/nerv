---
references:
  - 01-problem/clemvion-analysis.md
  - 01-problem/pain-points.md
  - 02-research/spec-driven-development.md
  - 02-research/agent-orchestration.md
  - 02-research/collab-platforms.md
  - 02-research/integration-tech.md
  - 03-proposal/vision.md
  - 03-proposal/architecture.md
  - 03-proposal/data-model.md
  - 03-proposal/agent-integration.md
  - 03-proposal/spec-workflow.md
  - 03-proposal/ui-wireframes.md
  - 03-proposal/roadmap.md
  - 04-mvp/scope.md
  - 04-mvp/codebase.md
  - 04-mvp/database.md
  - 04-mvp/api.md
  - 04-mvp/screens.md
  - 04-mvp/plugin.md
  - 04-mvp/importer.md
  - 04-mvp/backlog.md
  - README.md
  - ../README.md
  - ../AGENTS.md
---
# 용어 사전

> **요약** — 이 문서는 NERV 문서 세트·화면 문구·매뉴얼·스킬이 공유하는 **용어의 정본**이다 — 한국어와 영어 둘 다. 표제어 35개에 대해 한국어 채택어·영문·코드 식별자·한 줄 뜻을 정하고, 채택으로 **치환되는 옛 표현**과 그 파급을 함께 적는다. 말투(합쇼체/해라체)의 규칙도 여기에 둔다. 상태값의 목록과 결정 번호(D-01~D-14)·요구사항 번호(FR/NFR)는 여기가 아니라 [1.2 문제 정의와 요구사항](01-problem/pain-points.md)이 정본이고, 이 문서는 그 값들을 **무엇이라 부를지**만 정한다.
>
> 문서 버전 v0.4 · 2026-09-06 · HTML 파생본: [glossary.html](html/glossary.html)
>
> v0.4 변경(2026-09-06 — 치환 구역에 저장소 루트가 없었다, 정합성 대조 → 사람 지시): §1.1 이 "옛 표현은 저장소에 남아 있지 않다" 고 선언했는데 **루트 `README.md` 에 둘이 살아 있었다**(`HTML 판` · `안정 ID` — 둘 다 변경 기록이 아니라 본문). 구역 여섯이 `docs/` 와 `codebase/` 를 나눈 것이라 저장소 루트를 훑지 않은 것이 원인이다. 고치고, **다음 치환은 구역에 저장소 루트를 넣는다**는 것을 표 아래 적었다.
>
> v0.3 변경(2026-09-05 — 영어에는 정본이 없었다, 사람 지시): **영문 열을 정본으로 세우고 §2.6 을 신설한다.** 한국어를 고치는 동안 영어는 아무도 세지 않았고, 같은 모양의 결함이 넷 있었다 — ① 영문 매뉴얼이 발견의 처분을 **화면에 없는 낱말** `disposition` 으로 불렀다(화면은 `Resolution`). ② **철자가 기능 경계로 갈렸다** — 카탈로그의 `invite`·`onboarding` 은 -ise, 나머지는 -ize 였고 `settings.members.org_wide`("Whole organization")와 `invite.scope_org`("Whole organisation")가 **같은 화면에서 다른 철자**였다. ③ 한국어가 방금 끝낸 판/버전 문제가 **`revision`/`version` 으로 그대로** 남아 있었다. ④ `scope` 가 영어에서는 **네 뜻**을 날랐다(그래프 뷰가 하나 더). 71곳을 옮겼다 — 처분 4 · 철자 28 · 버전 24 · 수식어 15 — 코드 식별자와 임포터의 `disposition` 필드, MCP 프로토콜 `revision` 은 건드리지 않았다.
>
> v0.2 변경(2026-09-05 — 결정을 실물에 반영, 사람 지시): 채택 여덟을 **1,372곳에 적용**하고 §1.1 을 추정치에서 **실측치로** 고쳤다. 적용하며 뜻이 하나 더 나왔다 — **판에는 회차 뜻도 있다**("임베딩 한 판"). 그래서 판은 셋으로 갈렸고(버전 279 · 판본 10 · 회차와 판본으로 남긴 34) §2.2 에 그 사실을 적었다. **조사가 함께 바뀐다**는 것도 여기서 드러났다 — 스코프는 모음으로 끝나고 권한·소속은 자음으로 끝나 `스코프를` → `권한을` 처럼 조사까지 가야 한다(§4.1). 자동 치환이 **문서 자신의 변경 기록을 훼손한 자리 여섯**을 되돌렸다.
>
> v0.1 신설(2026-09-05 — 정본이 선언만 있고 실물이 없었다, 용어 검토 → 사람 결정): [1.2 pain-points](01-problem/pain-points.md) §4.1 이 "상태 어휘는 **용어 사전**을 그대로 따른다(재정의 금지)" 고 적어 두었는데 **그 문자열이 저장소 전체에서 그 한 줄뿐**이었다 — 가리키는 곳에 아무것도 없었다. 그래서 같은 것이 두 이름으로 자랐고(기준선/베이스라인), 한 이름이 세 가지를 가리켰고(스코프), 뜻이 서지 않는 직역이 남았다(안정 ID·리스). 이 문서가 그 자리다.

---

## 0. 이 문서의 자리

### 0.1 무엇의 정본인가

| 이 문서가 정하는 것 | 이 문서가 정하지 않는 것 |
| --- | --- |
| 개념을 **한국어와 영어로 무엇이라 부를지** | 그 개념의 **정의와 규칙** — 각 주제의 정본 문서가 갖는다 |
| 옛 표현 → 채택어의 **치환 방향** | 상태값의 **목록** — [1.2 §4.1](01-problem/pain-points.md) |
| 화면·문서의 **말투** | 결정 번호 `D-01`~`D-14`, 요구사항 번호 `FR`/`NFR` — [1.2](01-problem/pain-points.md) |
| 영어의 **철자 계열과 수식어** (§2.6) | 영어의 문법·문장 — 이 사전은 낱말만 본다 |
| 코드 식별자를 **번역하지 않는다는 경계** | API·DB 의 영문 식별자 자체 — [4.3](04-mvp/database.md)·[4.4](04-mvp/api.md) |

### 0.2 코드 식별자는 번역 대상이 아니다

`scope` · `baseline` · `lease_expires_at` · `spec_versions` · `AGENT_SCOPES` 는 **API·DB 계약**이다. 이 사전이 움직이는 것은 **사람과 에이전트가 읽는 한국어**뿐이고, 식별자는 그대로 둔다. 그래서 아래 치환표의 어느 행도 마이그레이션을 요구하지 않는다.

### 0.3 우선순위

사람과 에이전트가 **읽는 곳이 먼저**다 — 화면 문구(`packages/schema/src/i18n/ko.ts`) · 제품 매뉴얼 · 플러그인 스킬. 명세 md 와 html 파생본은 그 뒤에 따라온다. 스킬은 문서가 아니라 **모델이 읽는 규약**이라 화면과 같은 무게로 다룬다([AGENTS.md](../AGENTS.md) 구현 규약 6).

---

## 1. 결정 아홉 (2026-09-05)

### 1.1 치환표

파급은 실측이다. 구역은 겹치지 않게 나눴고, 다른 말에 박히는 낱말(`릴리스`·`리스트`·`판단`)은 앞뒤를 배제해 셌으며, 생성물(`.tsbuild`·`dist`·`node_modules`)은 세지 않았다.

| 옛 표현 → 채택어 | 화면 | 매뉴얼 | 스킬 | 코드 | 명세 | html | 합계 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 베이스라인 → **기준선** | 8 | 1 | 2 | 41 | 65 | 48 | 165 |
| 워크플로 → **워크플로우** | 0 | 0 | 0 | 2 | 68 | 65 | 135 |
| 스코프 → **권한 · 소속 · 작업 범위** | 11 | 10 | 3 | 154 | 206 | 167 | 551 |
| 판 → **버전**(279) · **파생본**(10) | 13 | 29 | 5 | 111 | 86 | 45 | 289 |
| 안정 ID · 안정 키 → **번호**(화면) · **고정 ID**(그 밖) | 6 | 0 | 2 | 42 | 80 | 68 | 198 |
| 리스 → **남은 시간 · 점유 시간** (사람이 읽는 곳만) | 3 | 15 | — | — | — | — | 18 |
| 클레임 → **맡음** (보드 칸 이름만) | 1 | 0 | 0 | 0 | 0 | 0 | 1 |
| 핀하다 → **묶다** | 1 | 0 | 0 | 3 | 6 | 5 | 15 |
| **합계** | **43** | **55** | **12** | **353** | **511** | **398** | **1,372** |

사람과 에이전트가 읽는 자리는 **110곳**(화면 43 · 매뉴얼 55 · 스킬 12)이고, 나머지 1,262곳은 코드 주석과 명세·파생본이다.

**이 표는 실제로 옮긴 수다**(2026-09-05 반영 완료 — v0.2). 옛 표현은 이 문서의 인용과 [README](README.md)·[AGENTS.md](../AGENTS.md)의 변경 기록 넷 말고는 저장소에 남아 있지 않다.

> **구역에 저장소 루트가 없었다**(2026-09-06 보완). 위 표의 여섯 구역(화면·매뉴얼·스킬·코드·명세·html)은 `docs/` 와 `codebase/` 를 나눈 것이라 **저장소 루트의 `README.md` 를 훑지 않았다** — 그래서 "남아 있지 않다" 는 선언이 참이 아닌 채로 하루를 지났다(`HTML 판` 한 곳 · `안정 ID` 한 곳, 둘 다 변경 기록이 아니라 본문). 고쳤고, **다음 치환은 구역에 저장소 루트를 넣는다** — 저장소를 처음 여는 사람이 가장 먼저 읽는 문서다.

### 1.2 부분 채택 셋 — 어디까지인지 못 박는다

- **리스**는 사람이 읽는 20곳만 푼다. 명세·코드 주석·스킬은 `lease_expires_at` 과 짝을 맞춰 **리스를 개념어로 남긴다** — 대신 §2.3 이 그 뜻을 한 줄로 정의하는 것이 조건이다.
- **클레임**은 낱말을 그대로 둔다. 바꾸는 것은 보드 칸 이름 `status.task.claimed` **한 칸**이다(§2.3).
- **판 → 버전**은 **버전 뜻에만** 적용한다. 판본 뜻은 별도 표제어이고 이번에 **HTML 판 → HTML 파생본**으로 옮겼다(§2.2).

### 1.3 이미 끝난 개명

| 옛 이름 | 채택어 | 결정일 | 남은 자리 |
| --- | --- | --- | --- |
| 승인함 | **받은 요청** | 2026-08-27 (README v1.45) | 화면·매뉴얼·스킬 0건 · 명세 15곳 |

---

## 2. 사전

한 줄 뜻은 **이 저장소 안에서의 쓰임**이다. `코드` 열은 데이터베이스·API 식별자로 번역하지 않는다.

### 2.1 조직 · 사람 · 권한

| 한국어 | 영문 | 코드 | 뜻 |
| --- | --- | --- | --- |
| **조직** | organization | `organizations` | 테넌트의 가장 바깥 칸. 사람·프로젝트·결제가 여기에 매인다 |
| **프로젝트** | project | `projects` | 스펙·작업·리뷰가 사는 단위. 주소의 `slug` 가 이것을 가리킨다 |
| **역할** | role | `member_role` | 사람이 조직·프로젝트에서 무엇을 할 수 있는지 정하는 여섯 값 — `admin`·`planner`·`designer`·`developer`·`qa`·`viewer` |
| **소속** ← 스코프 | membership scope | `memberships.project_id` | 멤버가 **조직 전체**인지 **특정 프로젝트**인지 — 사람이 어디까지 보이는가 |
| **권한** ← 스코프 | scope | `AGENT_SCOPES`·`HUMAN_ONLY_SCOPES` | 토큰이 할 수 있는 일. `resource:action` 표기로 10종이고, 그 위에 토큰이 가질 수 없는 **사람 전용 권한** 둘(`spec:approve`·`approval:decide`)이 있다 |
| **토큰** | personal access token | `agent_tokens` | 에이전트가 사람을 대신해 들고 다니는 열쇠. 발급 직후 한 번만 원문이 보이고, 역할보다 넓어질 수 없다. 약어 `PAT` 는 명세에서만 쓰고 화면에는 내지 않는다 |
| **초대** | invitation | `invitations` | 이메일·역할·소속을 정해 만든 링크. 받는 사람이 로그인하면 자동으로 수락된다 |

**스코프 세 갈래를 나눈 이유.** 한 낱말이 같은 설정 화면의 두 탭에서 서로 다른 것을 가리키고 있었다 — 토큰 탭의 "스코프" 는 권한, 멤버 탭의 "스코프" 는 소속이었고, 오류 문구 "이미 같은 스코프의 멤버입니다" 는 그 문장만 보고 어느 쪽인지 알 수 없었다. 세 번째 뜻(클레임의 범위)은 **화면이 이미 "범위" 로 갈아탄 뒤**였다(`event.claim.conflict_warn` = "범위 겹침 경고") — 채택어 "작업 범위" 는 새 말이 아니라 그 갈라짐을 사전이 따라잡는 것이다.

### 2.2 스펙

| 한국어 | 영문 | 코드 | 뜻 |
| --- | --- | --- | --- |
| **스펙** | spec | `specs` | 단일 진실로 관리되는 문서 한 편. 화면 내비게이션의 이름이기도 하다 |
| **버전** ← 판 | version | `spec_versions` | 스펙의 한 시점. 초안 → 검토 중 → 승인됨 → 대체됨·폐기됨의 다섯 상태를 갖는다. 승인된 버전은 고칠 수 없고, 고치려면 **다음 버전**을 시작한다 |
| **판** | edition · pass | — | 두 가지로 남는다 — ① **판본**: 같은 내용을 다른 형태로 낸 것("전체 화면 판"·"markdown 판"). ② **회차**: 한 번 도는 것("임베딩 한 판" — `NERV_EMBED_PASS_MS`). 둘 다 **버전과 다른 말**이다 — 아래 참고 |
| **요구사항** | requirement | `requirements` | 스펙 안의 검증 가능한 한 줄. EARS 문형을 쓰고 `REQ-…` 번호를 갖는다 |
| **기준선** ← 베이스라인 | baseline | `baselines` | 문서를 어느 버전으로 읽을지 정해 둔, 이름 붙은 묶음. 고르지 않으면 각 문서를 최신 승인본으로 읽는다 |
| **워크플로우** ← 워크플로 | workflow | `spec_version_status` | 버전이 상태를 옮겨 가는 규칙과, 그 사이에 놓인 승인 게이트 |
| **변경 요청** | change request | `change_requests` | 승인된 문서를 고치자는 제안. 위험도(`low`·`normal`·`high`)와 출처(사람·에이전트·스펙 드리프트)를 단다 |
| **고정 ID** ← 안정 ID·안정 키 | stable id · stable key | `stable_key` | 문서를 옮기거나 이름을 바꿔도 변하지 않는 값 — `SPC-…`·`REQ-…`·`TSK-…` |

**"판" 과 "버전" 을 가른 이유.** 전수 323곳 중 **34곳이 버전이 아니었다** — 판본 뜻 아홉과 **회차 뜻 스물다섯**이다. 회차 뜻은 반영하면서야 드러났다(`임베딩 한 판의 시간 상한`) — 기계적으로 밀었다면 "임베딩 한 버전의 시간 상한" 이 됐을 것이다. 판본 뜻의 가장 분명한 증거는 모든 문서의 머리글이다 — `> 문서 버전 v0.3 · 2026-09-05 · HTML 판: [...]` 은 **한 줄 안에 둘이 함께 있고 서로 다른 것을 가리킨다**. 일괄 치환하면 "문서 버전 … HTML 버전:" 이 되어 뒤엣것이 무엇인지 알 수 없어진다. 그래서 버전 뜻 **279곳**만 옮기고, `HTML 판`·`html 판` **40곳**은 **"HTML 파생본"** 으로 바꿨다 — 이 저장소가 이미 md 를 원본, html 을 파생본이라 부르기 때문이다([README](README.md) 관리 규약).

**"고정 ID" 와 화면의 "번호".** "안정" 은 한국어에서 *흔들리지 않는 상태*를 뜻해, 실제 뜻인 "옮겨도 변하지 않는 값" 이 이름에서 오지 않았다. 문서가 그 뜻을 **매번 다시 설명하던 것**이 증거다(`database.md`: "안정 ID. 이동·개명해도 참조 불변"). **화면에서는 "번호" 로 충분하다** — 사람은 `SPC-…` 를 이미 번호로 읽는다. "고정 ID" 는 스펙·요구사항·작업을 아울러야 하는 **명세의 개념어**다.

### 2.3 작업

| 한국어 | 영문 | 코드 | 뜻 |
| --- | --- | --- | --- |
| **작업** | task | `tasks` | 사람이나 에이전트가 실제로 수행하는 단위. `TSK-…` 번호를 갖는다 |
| **클레임** | claim | `claims` | 한 세션이 작업을 **쥐는 것**. 원자적이라 둘이 동시에 같은 작업을 쥘 수 없다(`D-04`) |
| **맡음** ← 클레임 | claimed | `task_status` | 보드에서 **클레임된 상태**를 가리키는 칸 이름 — 아래 참고 |
| **리스** | lease | `lease_expires_at` | 클레임이 **살아 있는 시간**. 기본 30분이고, 하트비트로 갱신하지 않으면 만료돼 작업이 풀린다 |
| **남은 시간 · 점유 시간** ← 리스 | lease | — | 사람이 읽는 자리에서 "리스" 대신 쓰는 말. 배지는 "남은 시간 {remaining}", 오류는 "점유 시간이 지났거나…" |
| **작업 범위** ← 스코프 | claim scope | `claims.spec_ids`·`file_globs` | 클레임이 선언하는 **건드릴 스펙과 파일**. 겹침 판정의 입력이다 |
| **하트비트** | heartbeat | `heartbeat_at` | 세션이 살아 있음을 알리는 주기 신호. 끊기면 무응답으로 넘어가고 클레임이 회수된다 |
| **지문** | content hash | `content_hash` | 읽은 내용이 그 사이에 바뀌지 않았음을 확인하는 값. **리스가 시간을 지키고 지문이 내용을 지킨다** |
| **증적** | evidence | `evidence_kind` | 작업이 실제로 끝났음을 가리키는 자료 — 코드 경로·테스트·PR·커밋·리뷰·사용 안내의 여섯 종 |

**"클레임" 은 두고 칸 이름만 바꾼 이유.** 낱말은 매뉴얼이 뜻을 풀어 주고(*"작업을 쥐는 것을 클레임이라고 합니다"*) 도구 이름(`nerv_task_claim`)·상태값·이벤트에 박혀 있어 그대로 둔다. 다만 보드 칸 이름은 이웃 여섯이 모두 상태인데(백로그·준비됨·진행 중·검토 중·완료·막힘) 그 한 칸만 명사였고, 영문은 이미 `Claimed` 로 상태다.

**"지문" 은 직역이 아니라 고른 은유다.** "해시" 와 섞여 쓰이지만 가리키는 층이 다르다 — **개념은 지문, 구현은 해시**.

### 2.4 리뷰 · 게이트

| 한국어 | 영문 | 코드 | 뜻 |
| --- | --- | --- | --- |
| **리뷰** | review | `reviews` | 코드·정합성·스펙 커버리지·머지 네 종의 검토. 사람이 아니라 **플랫폼의 개체**다(`D-07`) |
| **발견** | finding | `findings` | 리뷰가 집어낸 항목 하나. 심각도 셋(`critical`·`warning`·`info`)과 상태 넷(열림·수정됨·기각·유예)을 갖는다 |
| **처분** | resolution | `resolution_kind` | 발견을 어떻게 닫았는지 — 수정·기각·유예·에스컬레이션. 근거를 반드시 적는다 |
| **게이트** | gate | `gate_policies` | 사람의 승인이 있어야 지날 수 있는 지점. 위험 신호가 쌓이면 티어가 오른다 |
| **에스컬레이션** | escalate | `escalate_reason` | 에이전트가 스스로 판단하지 않고 사람에게 올리는 것. 사유 다섯 중 하나를 단다 |

### 2.5 승인 · 소통

| 한국어 | 영문 | 코드 | 뜻 |
| --- | --- | --- | --- |
| **받은 요청** ← 승인함 | inbox | `approvals` | 내 결정을 기다리는 것들이 모이는 곳. **알림과 다르다** — 알림은 이미 일어난 일이다 |
| **승인** | approval | `approval_decision` | 사람만 내릴 수 있는 결정 — 승인·반려·코멘트. 토큰에 위임되지 않는다(`D-08`) |
| **질문** | question | `questions` | 에이전트가 막혔을 때 사람에게 묻는 것. 사람이 답하거나, **만든 세션이 스스로 취소**할 수 있다 |
| **알림** | notification | `notifications` | 이미 일어난 일을 알리는 것. 즉시와 모아 보기 두 등급이 있다 |
| **세션** | agent session | `agent_sessions` | 에이전트가 일하는 한 번의 회차. 대기·활동 중·응답 대기·무응답·종료·오류의 여섯 상태. 사람의 로그인 세션과 이름이 겹치므로 **명세에서는 "에이전트 세션" 으로 늘여 쓴다** |
| **활동** | activity | `activities` | 프로젝트에서 벌어진 일의 시간순 기록. 감사 로그이자 사람이 읽는 피드다 |

### 2.6 영어 — 영문 열도 정본이다 (2026-09-05 신설)

위 표의 `영문` 열은 **참고가 아니라 정본**이다. 영문 카탈로그(`packages/schema/src/i18n/en.ts`)와
제품 매뉴얼 en 이 그것을 따른다. 규칙 셋을 함께 둔다.

| 규칙 | 정한 것 | 왜 |
| --- | --- | --- |
| **철자는 -ize 계열** | `organization` · `center` (❌ `organisation`·`centre`) | 코드 식별자가 이미 `organization`·`org_id`·`graph.panel.center` 다. 그전에는 카탈로그가 **기능 경계로 갈려** 있었다 — `invite`·`onboarding` 은 -ise, 나머지는 -ize |
| **`version`, `revision` 아님** | 스펙의 한 시점은 `version` | 한국어의 판 → 버전과 같은 결정이고 DB 도 `spec_versions` 다. **MCP 프로토콜의 `revision` 은 별개**라 그대로 둔다(`error.mcp.bad_revision`) |
| **`scope` 는 뜻마다 수식어** | 아래 넷 | 한국어는 세 낱말로 갈랐지만 영어의 `scope` 는 **첫 번째 뜻에서 API 식별자 자체**라 버릴 수 없다 |

| 뜻 | 영어 | 한국어 |
| --- | --- | --- |
| 토큰이 할 수 있는 일 | **`scope`** (수식어 없음 — `AGENT_SCOPES`·`resource:action`) | 권한 |
| 멤버가 조직 전체인지 프로젝트인지 | **`membership scope`** | 소속 |
| 클레임이 선언한 스펙·파일 | **`declared scope`** | 작업 범위 |
| 관계 그래프가 보여 주는 폭 | **`view scope`** | 보기 범위 |

`declared scope` 는 새로 지은 말이 아니다 — 화면이 이미 `session.scope` = "Declared scope" 로 쓰고 있었고,
그 수식어를 겹침 문구까지 넓힌 것이다.

**영문 매뉴얼이 화면에 없는 낱말을 쓰지 않는다.** `reviews.md` 가 발견의 처분을 `disposition` 이라 부른 적이
있는데(넷) 카탈로그에는 0건이었고 화면은 `Resolution`·`Resolve finding` 이었다 — 찾는 사람이 찾지 못한다.
**임포터 리포트의 `disposition` 필드는 다른 개념**이라 그대로 둔다(`apps/cli/src/run.ts`).

---

## 3. 말투

### 3.1 규칙

**읽는 쪽이 정한다.**

| 읽는 쪽 | 말투 | 키 |
| --- | --- | --- |
| 사람이 보는 화면 | **합쇼체** (`…합니다`) | 위 둘을 뺀 전부 |
| 에이전트·CLI 가 받는 것 | **해라체** (`…한다`) | `mcp.*` · `agent.*` · `cli.*` · `import.*` |
| 생성 문서 | **해라체** | `export.*` (예: `llms.txt`) |

### 3.2 왜 규칙이 필요했나

규정이 저장소 어디에도 없었다 — `docs/`·매뉴얼·[AGENTS.md](../AGENTS.md)·카탈로그 머리 주석 모두. 카탈로그는 규칙 셋(키 짓기·자리표시자·식별자 비번역)을 적어 두었는데 **말투만 빠져 있었고**, 그래서 설명·근거를 적는 문장이 문서의 말투를 그대로 끌고 들어왔다. 자국은 **화면 아홉**이었고, 의도가 아니라는 근거는 넷이다.

1. **같은 슬롯의 다수가 반대편이다.** 흐린 주석 자리(faint·mute·hint·title)의 한국어 153건 중 합쇼체 67 · 해라체 8. 주석의 말투였다면 이 비가 나오지 않는다.
2. **같은 컴포넌트 안에서 갈렸다.** `resolve-dialog.tsx` 의 `Field hint` 셋 중 둘만 해라체였고, `ko.ts` 의 **나란한 두 줄**이 `'요구사항은 그대로다'` / `'본문이 같습니다.'` 였다(같은 diff 뷰의 같은 자리).
3. **영문과 상관관계가 없다.** 해라체 여덟 중 여섯의 영문이 이웃과 같은 문장형(대문자+마침표)이었고, 반대로 합쇼체인 `reviews.resolve.spec_hint` 의 영문은 소문자·마침표 없음이었다. 두 언어가 함께 지키는 규칙이 아니었다.
4. **도입 커밋이 스스로 섞였다.** 카탈로그를 만든 커밋(`06ec9fca` — i18n 전면 적용)이 그 한 커밋에서 **합쇼체 176 · 해라체 7** 을 함께 넣었다. 규칙이었다면 그 커밋이 지켰다.

### 3.3 고치지 않은 셋

`import.requirement_not_found` · `import.task_not_found` 는 **웹이 그리지 않는다** — `apps/cli/src/run.ts` 가 `item.detail ?? t('cli.reason.load_failed')` 로 소비하므로 `cli.reason.*` 열아홉과 같은 자리다. `export.index_lead` 는 `llms.txt` 에 쓰이는 문서 본문이다. 셋 다 §3.1 의 규칙대로 해라체가 맞다.

---

## 4. 적용 규칙

### 4.1 일괄 치환이 안 되는 넷

| 낱말 | 박혀 있는 곳 | 거르는 법 |
| --- | --- | --- |
| 리스 | 릴**리스** · **리스**트 · 휴**리스**틱 | 앞의 `릴`·`휴`, 뒤의 `트`·`틱` 을 배제한다. 배제 전 950건 → 후 796건 |
| 워크플로 | **워크플로**우 의 앞부분 | 뒤에 `우` 가 오지 않는 것만 고른다 |
| 판 | **판**단 · **판**정 · **판**별 · 재**판** · 출**판** | 앞뒤가 한글이 아니거나 조사가 붙은 것만 고른다. 그러고도 판본 뜻이 남으므로 §2.2 를 함께 본다 |
| 클레임 | — | 바꾸는 것은 `status.task.claimed` **한 칸**이다. 나머지 944곳은 그대로 둔다 |

**조사가 함께 바뀐다.** `스코프`는 모음으로 끝나고 `권한`·`소속`은 자음으로 끝난다 — `스코프를`→`권한을` · `스코프는`→`권한은` · `스코프가`→`권한이` · `스코프와`→`권한과` · `스코프로`→`권한으로`. 낱말만 바꾸면 `소속를` 같은 것이 남는다. `판`→`버전`, `베이스라인`→`기준선` 은 둘 다 자음으로 끝나 조사가 그대로다.

**문서 자신의 변경 기록을 조심한다.** 옛 표현을 *인용하는* 문장이 있다 — "기준선/베이스라인 165곳", "판 → 버전", "한 이름이 세 가지를 가리켰다(스코프)". 일괄 치환은 그것도 바꾸어 **기록이 자기가 무엇을 고쳤는지 말하지 못하게** 만든다. 이 사전과 [README](README.md)·[AGENTS.md](../AGENTS.md)의 해당 줄은 치환에서 뺀다.

측정에서 **생성물은 뺀다** — `.tsbuild`·`dist`·`node_modules`. 넣고 세면 몇 건씩 부푼다.

### 4.2 규약이 요구하는 동반 변경

1. **md 와 html 은 같은 커밋에서**([README](README.md) 관리 규약 1). 파생본 400곳이 뒤처지면 그 자체가 결함이다.
2. **매뉴얼은 ko·en 두 벌**([AGENTS.md](../AGENTS.md) 구현 규약 5). 영문은 일관되므로 대개 손댈 것이 없지만, **장 제목이 바뀌면 카탈로그의 `help.ch.*`** 도 함께 간다 — `manual.spec.ts` 가 본다.
3. **스킬을 건드리면 버전을 올린다**([AGENTS.md](../AGENTS.md) 구현 규약 6 · `REQ-PLG-017`). `plugin.json` 의 `version`, 카탈로그 둘, `plugin/README.md`, 매뉴얼 ko·en 이 한 묶음이다. **같은 버전이면 이미 설치한 쪽은 옛 사본을 계속 읽는다.**
4. **`SKILL.md` 와 [4.6 플러그인](04-mvp/plugin.md)은 바이트 대조된다** — 한쪽만 고치면 `plugin-package.spec.ts` 가 막는다.
5. **표제어를 더하거나 채택어를 바꾸면** 이 문서의 버전과 [README](README.md) 의 문서 버전을 올린다(관리 규약 4).
