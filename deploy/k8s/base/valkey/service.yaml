# configmap 의 NERV_VALKEY_URL=redis://nerv-valkey:6379 가 이 이름을 계약한다.
apiVersion: v1
kind: Service
metadata:
  name: nerv-valkey
  labels: { app: nerv-valkey }
spec:
  selector: { app: nerv-valkey }
  ports:
    - { name: redis, port: 6379, targetPort: redis }
